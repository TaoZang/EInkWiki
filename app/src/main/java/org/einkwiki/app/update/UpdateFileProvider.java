package org.einkwiki.app.update;

import androidx.core.content.FileProvider;

/** Exposes only verified APKs under cache/updates to the system package installer. */
public final class UpdateFileProvider extends FileProvider {
}
